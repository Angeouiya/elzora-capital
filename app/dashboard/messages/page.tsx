"use client";import{useEffect,useState}from"react";import{InvestorShell}from"@/components/investor/InvestorShell";import{Card}from"@/components/ui/Card";import{Badge}from"@/components/ui/Badge";export default function Page(){const[n,setN]=useState<any[]>([]);useEffect(()=>{fetch('/api/notifications').then(r=>r.ok?r.json():[]).then(d=>setN(Array.isArray(d)?d:[])).catch(()=>{})},[]);return <InvestorShell title="Messages" subtitle="Informations contractuelles, opérationnelles et notifications importantes."><div className="space-y-3">{n.map(x=><Card key={x.id} padding="sm"><div className="flex items-start justify-between gap-3"><div><p className="font-semibold">{x.title}</p><p className="mt-1 text-sm text-[#101010]/60">{x.message}</p><p className="mt-2 text-xs text-[#101010]/35">{new Date(x.createdAt).toLocaleString('fr-FR')}</p></div>{!x.read&&<Badge variant="accent">Nouveau</Badge>}</div></Card>)}{!n.length&&<Card><p className="text-sm text-[#101010]/50">Aucun message.</p></Card>}</div></InvestorShell>}