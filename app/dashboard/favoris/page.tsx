"use client";import{useEffect,useState}from"react";import Link from"next/link";import{InvestorShell}from"@/components/investor/InvestorShell";import{Card}from"@/components/ui/Card";import{Button}from"@/components/ui/Button";import{formatFCFA,formatRate}from"@/lib/calculations";export default function Page(){const[o,setO]=useState<any[]>([]);useEffect(()=>{const ids=(()=>{try{return JSON.parse(localStorage.getItem('nexora_favorites')||'[]')}catch{return[]}})();fetch('/api/offers').then(r=>r.ok?r.json():[]).then(d=>setO((Array.isArray(d)?d:[]).filter((x:any)=>ids.includes(x.id)))).catch(()=>{})},[]);return <InvestorShell title="Favoris" subtitle="Offres enregistrées pour les revoir avant toute décision d'investissement."><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{o.map(x=><Card key={x.id}><p className="text-xs font-semibold uppercase tracking-wide text-[#507300]">{x.project.sector}</p><h2 className="mt-2 text-lg font-bold">{x.project.company.name}</h2><p className="mt-1 text-sm text-[#101010]/55">{x.project.title}</p><div className="my-4 grid grid-cols-2 gap-2"><div className="rounded-[12px] bg-[#EFFBDD] p-3"><p className="text-[10px] uppercase text-[#101010]/45">Conditions</p><b>{formatRate(x.rate)}</b></div><div className="rounded-[12px] bg-[#F5F5F3] p-3"><p className="text-[10px] uppercase text-[#101010]/45">Minimum</p><b>{formatFCFA(x.minTicket)}</b></div></div><Link href={`/offres/${x.id}`}><Button className="w-full">Voir l'offre</Button></Link></Card>)}{!o.length&&<Card><p className="text-sm text-[#101010]/50">Vous n'avez encore enregistré aucune offre.</p></Card>}</div></InvestorShell>}