"use client";import{useEffect,useState}from"react";import{EntrepriseShell}from"@/components/entreprise/EntrepriseShell";import{Card}from"@/components/ui/Card";import{StatusBadge}from"@/components/ui/StatusBadge";import{formatFCFA}from"@/lib/calculations";export default function Page(){const[p,setP]=useState<any[]>([]);useEffect(()=>{fetch('/api/projects').then(r=>r.ok?r.json():[]).then(d=>setP(Array.isArray(d)?d:[])).catch(()=>{})},[]);return <EntrepriseShell title="Rapports" subtitle="Suivez les dossiers et financements qui doivent faire l'objet d'un compte rendu."><div className="grid gap-4 md:grid-cols-2">{p.map(x=><Card key={x.id}><div className="flex items-start justify-between gap-3"><div><h2 className="font-bold">{x.title}</h2><p className="mt-1 text-sm text-[#101010]/50">{formatFCFA(x.requestedAmount)}</p></div><StatusBadge status={x.status}/></div><p className="mt-4 text-sm leading-relaxed text-[#101010]/60">Les rapports d'exécution doivent documenter l'usage des fonds, l'avancement, les écarts budgétaires et tout incident significatif.</p></Card>)}{!p.length&&<Card><p className="text-sm text-[#101010]/50">Aucun dossier à reporter pour le moment.</p></Card>}</div></EntrepriseShell>}