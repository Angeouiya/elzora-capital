"use client";import{useEffect,useState}from"react";import{EntrepriseShell}from"@/components/entreprise/EntrepriseShell";import{Card}from"@/components/ui/Card";import{Badge}from"@/components/ui/Badge";export default function Page(){const[n,setN]=useState<any[]>([]);useEffect(()=>{fetch('/api/notifications').then(r=>r.ok?r.json():[]).then(d=>setN(Array.isArray(d)?d:[])).catch(()=>{})},[]);return <EntrepriseShell title="Messages" subtitle="Communications opérationnelles liées aux dossiers, décisions et paiements."><div className="space-y-3">{n.map(x=><Card key={x.id} padding="sm"><div className="flex items-start justify-between gap-3"><div><p className="text-sm font-bold">{x.title}</p><p className="mt-1 text-sm leading-relaxed text-[#101010]/60">{x.message}</p></div>{!x.read&&<Badge variant="accent">Nouveau</Badge>}</div></Card>)}{!n.length&&<Card><p className="text-sm text-[#101010]/50">Aucune communication à afficher.</p></Card>}</div></EntrepriseShell>}