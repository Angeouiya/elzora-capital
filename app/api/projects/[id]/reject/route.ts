import { NextRequest, NextResponse } from "next/server";import{prisma}from"@/lib/db";import{auth}from"@/lib/auth";export async function POST(req:NextRequest,{params}:{params:Promise<{id:string}>}){try{const session=await auth();if(!session)return NextResponse.json({error:'Non authentifié'},{status:401});if(session.user.role!=='ADMIN')return NextResponse.json({error:'Accès refusé'},{status:403});const{id}=await params;const body=await req.json();const reason=typeof body.reason==='string'?body.reason.trim():'';if(!reason)return NextResponse.json({error:'Le motif de refus est obligatoire.'},{status:400});const current=await prisma.project.findUnique({where:{id},include:{company:true}});if(!current)return NextResponse.json({error:'Projet introuvable'},{status:404});if(!['SUBMITTED','UNDER_REVIEW'].includes(current.status))return NextResponse.json({error:"Le dossier n'est pas dans un état refusable."},{status:409});const project=await prisma.project.update({where:{id},data:{status:'REJECTED',rejectionReason:reason}});await prisma.notification.create({data:{userId:current.company.userId,type:'DECISION',title:'Décision sur votre dossier',message:`Votre dossier a été refusé. Motif : ${reason}`}});await prisma.auditLog.create({data:{userId:session.user.id,action:'PROJECT_REJECTED',entity:'Project',entityId:id,details:reason}});return NextResponse.json(project)}catch{return NextResponse.json({error:'Erreur serveur'},{status:500})}}
